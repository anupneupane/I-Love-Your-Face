
<h1>here's a small picture</h1>
<img src="<%= @photo.image.url(:thumb) %>">
<br>
<h1>and the same one bigger</h1>
	<img src="<%= @photo.image.url(:small) %>">
<br>
<h1>and the same one bigger</h1>
	<img src="<%= @photo.image.url(:large) %>">
<br>

